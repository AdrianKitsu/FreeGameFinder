import styled from "styled-components";

const AllPosts = () => {
  return (
    <>
      <Wrapper>All Posts</Wrapper>
    </>
  );
};

const Wrapper = styled.div`
  display: flex;
  justify-content: center;
  margin-left: auto;
  margin-right: auto;
`;

export default AllPosts;
